/// 视频在播放器视口中的展示模式。
///
/// - [cover]：沿用现有短视频信息流的铺满策略，可能会裁切画面；
/// - [contain]：完整展示视频内容，必要时在上下或左右留白；
/// - [adaptive]：根据视频宽高比和当前视口尺寸自动选择更合适的展示方式。
enum VideoDisplayMode {
  /// 铺满视口，允许裁切。
  cover,

  /// 完整展示视频，不允许裁切。
  contain,

  /// 按规则自动选择完整展示策略。
  adaptive,
}
